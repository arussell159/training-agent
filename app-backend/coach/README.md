# Coach instructions and resources

`PROJECT_INSTRUCTIONS.md` is the exact AI Coach Instructions supplied by the athlete on 2026-09-17. It is loaded server-side and included unchanged in the `instructions` field of every OpenAI Responses request, including tool continuations and new conversations. Do not replace it with the shorter web-chat contract or reformat it automatically.

The coach's resources are live reads, not duplicated attachments:

- The full official `CrankAddict/section-11/SECTION_11.md` is fetched and indexed without rewriting or dropping text. Core behavioral, validation, input-trust and source-hierarchy sections are included verbatim in every request. All remaining sections are available through `read_protocol_sections`; the hierarchy uses that upstream revision. The four athlete-supplied report templates are deployed unchanged in `reports/`, each with its own SHA-256 revision, and used by both chat reference reads and report buttons.
- The complete private `arussell159/my-training-data` → `main` → `DOSSIER.md` and `latest.json` are fetched at the start of every question, pinned to the same commit. The dossier is supplied in full as athlete context. Refreshing data within a response also re-reads the dossier.
- The supplied protocol v11.69 and dossier revision 1.0 (reviewed 2026-09-17) were verified to match those GitHub sources during setup, ignoring only line endings and surrounding whitespace. The dossier's declared official location is the private repository above. Future GitHub edits are read automatically; the uploaded copies are not competing runtime sources.

The dossier is never copied into this application repository or the frontend bundle. The persistent prompt does not grant tools. Source tools remain read-only; the additional `propose_calendar_workouts` tool prepares an exact preview through the data repository's GitHub workflow. Only the athlete's authenticated **Add to Intervals.icu** button can confirm that stored plan. The model cannot write directly, edit the dossier, execute code, or search the web. The app login protects coaching, proposals and their source data.

Calendar deployment assets are in `github/`. Copy the unchanged official `push.py` to the data repo root and the adapted `push-workout.yml` to `.github/workflows/`. The workflow uses the same Intervals.icu secrets as sync. Its additions pass all inputs as data (no shell eval), name each app run using its proposal ID and confirmation phase, and expose explicit preview/applied/not_applied/unknown steps. Do not remove the `training-agent-calendar-v1` contract marker or change those result names without updating the application adapter and tests. The app dispatches only `command=push`; other manual upstream commands are not model tools. `confirm=false` is the default. Re-running a confirmed workflow is blocked, since its previous outcome must be checked first.
